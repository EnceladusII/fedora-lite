# =========================
# Fedora Lite .env.example
# =========================
# Copy this file to .env and edit values as needed.
# All values are read by scripts in ./scripts

# ----------- Core -----------
GPU=auto            # auto|amd|nvidia|intel
DM=tuigreet           # tuigreet
TARGET_USER="$USER" # main login user
DEFAULT_SHELL=fish  # your setup uses fish
TERMINAL=foot       # preferred terminal emulator

# -------------- Env --------------

# --- Locals ---
LANG_DEFAULT="en_US.UTF-8"   # Interface language
FORMATS="fr_FR.UTF-8"        # Formats (dates, metrics, temperatures, 24h)
CLOCK_FORMAT="24h"

# --- Layout ---
KEYBOARD_LAYOUT="us"     # ex: fr, us
KEYBOARD_VARIANT=" "    # ex: intl, oss, nodeadkeys… (laisser vide si aucune)
KEYBOARD_OPTIONS=" "      # ex: "caps:escape"

# --- DM ---
THEME="matrix"          # minimal/matrix/doomsfire/colormix

# ----------- Experience -----------
INSTALL_DEV=1       # install coding tools (git, python, containers…)
INSTALL_GAMING=0    # install gaming stack (steam, xpadneo ...)
REMOVE_PLYMOUTH=1   # 1 = remove boot splash for speed

# ----------- NVIDIA -----------
NVIDIA_USE_AKMOD=1  # 1=akmod (default, recommended on Fedora), 0=kmod
NVIDIA_ALLOW_WL=1   # enable nvidia-drm KMS for Wayland/Hyprland

# ----------- AI / HPC -----------
INSTALL_CUDA=1          # install CUDA toolkit (NVIDIA only)
CUDA_REPO=official      # official|rpmfusion|skip

INSTALL_ROCM=1          # install ROCm stack (AMD only, best effort)
ROCM_REPO=amd           # amd|copr|skip

INSTALL_OPENGL_DEV=1    # OpenGL + Mesa dev headers/libs for ML, viz

# Your Hyprland + dotfiles repo (fish will bootstrap them)
DOTS_REPO="https://github.com/EnceladusII/caelestia-fedora.git"
DOTS_BRANCH=fedora        # or the branch you want
DOTS_SETUP_SCRIPT="install.fish"  # entrypoint in your repo

# =========================
# End of .env.example
# =========================
